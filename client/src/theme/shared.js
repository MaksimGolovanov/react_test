// src/theme/shared.js

// ========== ОБЩИЕ ТОКЕНЫ (НЕ ЗАВИСЯТ ОТ ТЕМЫ) ==========
// Эти параметры влияют на скругления, размеры шрифтов и семейство шрифтов во всём приложении.
// Они будут объединены с токенами конкретной темы (например, с цветами).
export const commonTokens = {
  token: {
    // Базовое скругление углов для большинства компонентов (кнопки, поля ввода, карточки)
    borderRadius: 8,
    // Увеличенное скругление для крупных элементов (модальные окна, крупные карточки)
    borderRadiusLG: 12,
    // Уменьшенное скругление для мелких элементов (теги, бейджики, маленькие кнопки)
    borderRadiusSM: 4,

    // Базовый размер шрифта (основной текст)
    fontSize: 14,
    // Крупный размер шрифта (заголовки, крупные кнопки)
    fontSizeLG: 16,
    // Мелкий размер шрифта (подсказки, второстепенный текст)
    fontSizeSM: 12,

    // Семейство шрифтов – будет применяться ко всем текстовым элементам
    fontFamily: "'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', sans-serif",
  },
};

// ========== НАСТРОЙКИ КОМПОНЕНТОВ ДЛЯ СВЕТЛЫХ ТЕМ ==========
// Здесь задаются стили для конкретных компонентов Ant Design, характерные для светлого оформления.
// В самих темах эти значения могут быть частично переопределены (например, изменён siderBg).
export const lightComponents = {
  // Компонент Layout (основная структура страницы)
  Layout: {
    headerBg: '#FFFFFF',      // Фон шапки (header)
    headerColor: '#1F1F1F',   // Цвет текста в шапке
    siderBg: '#FFFFFF',       // Фон боковой панели (сайдбар)
    bodyBg: '#F5F5F5',        // Фон основного контента (область вокруг карточек)
  },
  // Компонент Menu (навигационное меню, обычно в сайдбаре)
  Menu: {
    itemBg: 'transparent',        // Фон обычного пункта меню (прозрачный, чтобы просвечивал фон сайдбара)
    itemColor: '#4B4B4B',         // Цвет текста пункта меню
    itemHoverBg: '#F5F5F5',       // Фон пункта при наведении мыши
    itemSelectedBg: '#E6F4FF',    // Фон выбранного (активного) пункта меню
    // itemSelectedColor и itemHoverColor обычно наследуются из глобального colorPrimary,
    // но могут быть переопределены в конкретной теме
  },
  // Компонент Button (кнопки всех типов)
  Button: {
    defaultBg: '#FFFFFF',                 // Фон обычной кнопки (type="default")
    defaultBorderColor: '#E8E8E8',        // Цвет границы обычной кнопки
    defaultHoverBg: '#F5F5F5',            // Фон обычной кнопки при наведении
    borderRadius: 8,                      // Скругление углов кнопки (наследуется от глобального, но можно переопределить)
  },
  // Компонент Table (таблицы)
  Table: {
    headerBg: '#F9F9F9',          // Фон заголовков столбцов
    headerColor: '#1F1F1F',       // Цвет текста заголовков
    headerBorderRadius: 8,        // Скругление верхних углов таблицы
    rowHoverBg: '#F5F5F5',        // Фон строки при наведении
    borderColor: '#F0F0F0',       // Цвет границ таблицы
  },
  // Компонент Card (карточки)
  Card: {
    colorBgContainer: '#FFFFFF',  // Фон карточки
    borderRadiusLG: 12,           // Скругление углов карточки (используется увеличенное)
    boxShadow: '0 1px 2px 0 rgba(0, 0, 0, 0.03)', // Лёгкая тень для карточки
  },
  // Компонент Modal (модальные окна)
  Modal: {
    borderRadiusLG: 16,           // Скругление углов модального окна (чуть больше, чем у карточек)
    contentBg: '#FFFFFF',         // Фон содержимого модального окна
  },
  // Компонент Input (поля ввода текста)
  Input: {
    colorBgContainer: '#FFFFFF',  // Фон поля ввода
    colorBorder: '#E8E8E8',       // Цвет границы поля ввода
    borderRadius: 8,              // Скругление углов поля ввода
  },
  // Компонент Select (выпадающие списки)
  Select: {
    colorBorder: '#E8E8E8',       // Цвет границы выпадающего списка (аналогично Input)
    borderRadius: 8,              // Скругление углов
  },
};

// ========== НАСТРОЙКИ КОМПОНЕНТОВ ДЛЯ ТЁМНЫХ ТЕМ ==========
// Аналогичные настройки, но для тёмного оформления – более тёмные фоны, полупрозрачные границы,
// специальные эффекты при наведении.
export const darkComponents = {
  Layout: {
    headerBg: '#1A1F2C',          // Тёмный фон шапки
    headerColor: '#E8E8E8',       // Светлый текст в шапке
    siderBg: 'linear-gradient(to bottom, #002e3d, #004c5e)', // Градиентный фон сайдбара (фирменный для Газпрома)
    bodyBg: '#0F131C',            // Очень тёмный фон контента
  },
  Menu: {
    itemBg: 'transparent',        // Прозрачный фон – просвечивает градиент сайдбара
    itemColor: '#D0D0D0',         // Светло-серый текст пунктов меню
    itemHoverBg: 'rgba(0, 255, 255, 0.1)',  // Полупрозрачный циановый фон при наведении (неоновый эффект)
    itemSelectedBg: 'rgba(250, 146, 47, 0.15)', // Полупрозрачный оранжевый фон для выбранного пункта
  },
  Button: {
    defaultBg: 'rgba(255, 255, 255, 0.06)',   // Полупрозрачный фон обычной кнопки
    defaultBorderColor: 'rgba(255, 255, 255, 0.15)', // Полупрозрачная граница
    defaultHoverBg: 'rgba(255, 255, 255, 0.12)',    // Чуть более яркий фон при наведении
    borderRadius: 8,
  },
  Table: {
    headerBg: '#1A1F2C',          // Тёмный фон заголовков таблицы
    headerColor: '#E0E0E0',       // Светлый текст
    headerBorderRadius: 8,
    rowHoverBg: 'rgba(250, 146, 47, 0.1)', // Полупрозрачный оранжевый при наведении на строку
    borderColor: '#2C3345',       // Тёмные границы
  },
  Card: {
    colorBgContainer: '#1E2432',  // Тёмный фон карточки
    borderRadiusLG: 12,
    boxShadow: '0 4px 12px rgba(0, 0, 0, 0.3)', // Более глубокая тень для тёмной темы
  },
  Modal: {
    borderRadiusLG: 16,
    contentBg: '#1E2432',         // Тёмный фон содержимого модалки
  },
  Input: {
    colorBgContainer: '#141923',  // Очень тёмный фон поля ввода
    colorBorder: '#2C3345',       // Тёмная граница
    borderRadius: 8,
  },
  Select: {
    colorBorder: '#2C3345',
    borderRadius: 8,
  },
};